import './header.css';
import Nav from '../Nav/Nav';
import Hero from '../Hero/Hero';

function Header() {
  return (
    <header>
        <Nav />
        <Hero />
    </header>
  );
}

export default Header;